import { _classCallCheck, _createClass, _createSuper } from "#renderkid/tools";
import { _inherits } from "#renderkid/tools";

import _LineWrapper from "./_LineWrapper";

const _setPrototypeOf = Object.setPrototypeOf;

import { p } from "#utils";

const DefaultLineWrapper = /*#__PURE__*/ (function (_require) {
  _inherits(DefaultLineWrapper, _require);

  const _super = _createSuper(DefaultLineWrapper);

  function DefaultLineWrapper() {
    _classCallCheck(this, DefaultLineWrapper);

    return _super.apply(this, arguments);
  }

  _createClass(DefaultLineWrapper, [
    {
      key: "_render",
      value: function _render() {},
    },
  ]);

  return DefaultLineWrapper;
})(_LineWrapper);

export default DefaultLineWrapper;
